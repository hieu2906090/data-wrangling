function sum(values) {
  return values.reduce((prev, cur) => prev.value + cur.value, 0);
}


let testArr = [
    {name: 'Hieu', value: 10},
    {name: 'Hieu1', value: 100},
    {name: 'Hieu2', value: 122}
]

console.log([]);
